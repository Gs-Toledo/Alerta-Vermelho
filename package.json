{
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=@alerta-vermelho/react",
    "build": "npm run build --workspace=@alerta-vermelho/react",
    "start": "npm run preview --workspace=@alerta-vermelho/react",
    "test:react": "npm run test --workspace=@alerta-vermelho/react",
    "test:core": "npm run test --workspace=@alerta-vermelho/core"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3"
  }
}
